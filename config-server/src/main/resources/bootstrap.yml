server:
  port: 19011

spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/${profiles.active:dev}/

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:19001/eureka/,http://127.0.0.1:19002/eureka/

logging:
  file:
    path: logs
